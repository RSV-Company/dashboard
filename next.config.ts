import type { NextConfig } from "next";

const nextConfig: NextConfig = {
	images: {
		domains: ["images.unsplash.com","rsv-web.s3.eu-west-2.amazonaws.com"],
	},
	/* config options here */
};

export default nextConfig;
